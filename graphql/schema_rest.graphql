type Movie{
    id: Int!,
    title: String!,
    rating: Float!,
    summary: String!,
    language: String!,
    midium_cover_image: String!   
}

type Query{
    movies: [Movie]!
    movies2(limit: Int!, orderBy: String!): [Movie]!
}